# Baseline — depth-3 nesting (Task 0)

Recorded 2026-08-03 against `pipeline/depth-3-nesting`, base master `901f6cf0`.
No production code was changed for this task. Every command below was run exactly as
written in `.superpowers/sdd/2026-08-02-depth-3-nesting/task-0-brief.md`, serially, in
this worktree, against its dedicated test DB (`libli_d3n`).

## Step 1 — Group A gates (must be green on the unmodified tree)

| Gate | Result |
|---|---|
| `uv run pytest --verbosity=0` | **`4569 passed, 1 skipped, 771 deselected, 3030 warnings in 273.29s`** — exit 0 |
| `uv run ruff check .` | `All checks passed!` — exit 0 |
| `uv run ruff format --check .` | `815 files already formatted` — exit 0 |
| `uv run python manage.py makemigrations --check --dry-run` | `No changes detected` — exit 0 |
| `uv run pytest tests/test_i18n_po_health.py --verbosity=0` | `12 passed in 11.14s` — exit 0 |

**Baseline pass count N = 4569** (full non-e2e suite; 1 skip, 771 e2e tests deselected
by the default `-m 'not e2e'` addopt — e2e was not run separately, the brief's Group A
list does not ask for it here, only the five gates above).

All five Group A gates are green. No BLOCKED condition.

Note: `makemigrations --check --dry-run` printed a Django `RuntimeWarning` about a
failed migration-history consistency check (`baza danych "libli_d3n" nie istnieje` —
the DB doesn't persist outside test runs), but this is a warning on a side-channel
check, not the gate itself; the gate's actual output (`No changes detected`) and exit
code (0) are what the brief specifies, and both are clean.

## Step 2 — Group B phrases (must be PRESENT ≥1 now, and go to 0 after Task 10)

```
two container types                        1  -> content-editors.md:123
cannot hold another container               1  -> content-editors.md:128
dwa typy kontenerów                         1  -> content-editors.pl.md:133
może zawierać innego kontenera              1  -> content-editors.pl.md:140
nestable inside Tabs and Columns            2  -> content-editors.md:151, interactive-elements.md:9
wewnątrz Zakładek                           1  -> interactive-elements.pl.md:10
zagnieżdżalne w Zakładkach i Kolumnach      1  -> content-editors.pl.md:166
```

All seven phrases match, `nestable inside Tabs and Columns` matches the expected two
files. None are inert (all ≥1). Gate is live in both directions as designed.

## Step 3 — Load-bearing mechanism claims, verified by execution

Exact output of the Step-3 one-liner:

```
spoiler fields   : ['id', 'label', 'body', 'elements']
spoiler has .data: False
NESTABLE count   : 19
SPOILER count    : 14
subset invariant : True
tabs+2col in ser : True
registry keys    : ['TabsElement', 'TwoColumnElement']
Element.ordering : ['order', 'pk']
```

`spoiler has .data` is `False`, as required. **Not BLOCKED.** Every other claim in the
spec's mechanism table (19 nestable keys, 14 spoiler-child keys, subset invariant holds,
`tabs`/`two_column` already in `SERIALIZERS`, registry currently model-keyed by
`TabsElement`/`TwoColumnElement` only, `Element.Meta.ordering == ["order", "pk"]`) is
confirmed exactly as the spec states.

## Step 4 — Guardrail-inversion list, re-derived by search

Every grep in the brief was run against `tests/` and `courses/tests/`. Below is the
full reconciliation: every test site the greps surfaced that concerns the old depth-2 /
one-container-special-case assumption, cross-checked against the spec's 16-row table
(`docs/superpowers/specs/2026-08-02-depth-3-nesting-design.md:882-897`).

**Reconciled guardrail sites: 17 total. The spec's table lists 16; 1 was missing from
it** (`tests/test_tabs_registry.py`, row 17 below — the brief itself flags this exact
site in its grep commentary, quoting it as "found only by running the suite against the
diff" on the original slice; it re-surfaced here via the `status_code == 400` grep).

| # | Site | Spec listed? | Intended action |
|---|---|---|---|
| 1 | `tests/test_twocolumn_registry.py:15` — `"two_column" not in NESTABLE_TYPE_KEYS` | Yes | Invert to `in`; rename `test_two_column_not_nestable_itself` (name goes false) |
| 2 | `tests/test_twocolumn_registry.py:16` — `"twocolumn" not in NESTABLE_TYPE_KEYS` | Yes | **Leave unchanged** — pins the form-key/transfer-key split; do not invert alongside #1 |
| 3 | `tests/test_twocolumn_registry.py:47-48` — `test_resolve_scope_rejects_container_child_in_two_column`, `"tabs"` case (l.48) vs `"choicequestion"` case (l.50) | Yes | `"tabs"` case (l.47-48) inverts to accept (depth-1 parent); `"choicequestion"` (l.49-50) stays reject; rename test |
| 4 | `tests/test_tabs_form_views.py:119-131` — `test_non_nestable_child_type_raises` (`# tabs-in-tabs`, l.124) | Yes | Inverts to accept (parent at depth 1); rename |
| 5 | `courses/tests/test_spoiler_nesting.py:141-151` — `test_resolve_scope_rejects_disallowed_child_type_in_spoiler`, tuple `("tabs", "spoiler", "choicequestion")` | Yes (spec: "~149") | `"tabs"`/`"spoiler"` become legal; reject tuple reduces to `("choicequestion",)`; add matching accept case |
| 6 | `courses/tests/test_spoiler_nesting.py:154-165` — `test_spoiler_child_types_includes_interactive_leaves` | Yes | `SPOILER_CHILD_TYPES` deleted; convert to a `NESTABLE_TYPE_KEYS` table |
| 7 | `courses/tests/test_spoiler_nesting.py:184-195` — `test_resolve_scope_still_rejects_children_of_nested_spoiler` (leaf child of a depth-2 spoiler-in-spoiler) | Yes (spec: "~190") | Inverts: leaf child of depth-2 parent is now legal (clause 4 only blocks containers at depth ≥2) |
| 8 | `courses/tests/test_spoiler_nesting.py:210-224` — `test_resolve_scope_refuses_children_for_nested_spoiler` (spoiler nested in a **tab**, then given a child) | Yes | Inverts to accept — **direct guard of Purpose bullet 3**, the slice's headline shape |
| 9 | `courses/tests/test_spoiler_nesting.py:285-313` — `test_spoiler_add_menu_hides_disallowed_cards`, banned tuple `("html","spoiler","stepper","markdone","guessnumber")` (l.306-313) | Yes | All five now emitted; move to allowed list. The `banned_question` tuple (l.315-327) is unaffected (Questions group stays `{% if not nested %}`) |
| 10 | `courses/tests/test_spoiler_nesting.py:330-346` — `test_spoiler_add_menu_shows_allowed_interactive_cards`, `present.isdisjoint({"spoiler","stepper","markdone","guessnumber"})` (l.346) | Yes | All four become present; the question-card disjointness at l.348-350 is unchanged |
| 11 | `courses/tests/test_spoiler_nesting.py:381-399` — `test_tabs_add_menu_unaffected`, asserting `fillblankquestion` absent (l.394-399) | Yes | Inverts — `{% if nested %}` change makes it present; rename test ("unaffected" goes false) |
| 12 | `tests/test_tabs_editor_partial.py:79-90` — `test_nested_add_menu_offers_only_nestable_types`, `'data-add-type="tabs"' not in html` (l.87) | Yes | Inverts: Tabs offered at depth 1 is the point of the slice. Needs explicit int `depth` **and** `max_nest_depth` in the render context or the assertion cannot pass |
| 13 | `tests/test_tabs_transfer.py:135` — `_els(_tabs_el(), _child(), _child("e3", parent="e2"))  # depth > 1` | Yes | Middle element is `type_="text"` (default) — still rejected, but now by "parent is not a container", not depth. Silently stops testing depth; rebuild with a container middle element or retire |
| 14 | `tests/test_tabs_transfer.py:133-134` — `_child(type_="tabs")  # tabs in tabs` | Yes | Becomes valid (container child at depth 2); move from reject parametrize into the accept test |
| 15 | `courses/tests/test_spoiler_transfer.py:71-138` — `test_validate_nesting_accepts_spoiler_slot_and_rejects_depth2`, the `depth2` fixture (l.114-138) | Yes | The depth-2 half (`[tabs(top), spoiler(child), text(grandchild)]`) inverts to valid — transfer-side twin of Purpose bullet 3; rename test |
| 16 | `courses/tests/test_spoiler_transfer.py:141-160` — `test_validate_nesting_rejects_container_spoiler_child` | Yes | Inverts: `tabs` child of a top-level spoiler now legal (parent depth 1, container child depth 2); rename, rewrite the `:142-143` comment |
| 17 | `tests/test_tabs_registry.py:71-88` — `test_nested_add_of_a_blocked_type_is_400`, parametrized over `{"type": "choicequestion"}`, `{"type": "slidebreak"}`, `{"type": "tabs"}` (l.74-76) | **No** — missing from the spec's 16-row table | The `{"type": "tabs"}` case (l.76): parent join is a depth-1 tabs element, so a `tabs` child lands at depth 2 — now legal, must invert to `200`/accept. The `choicequestion` and `slidebreak` cases are unaffected (neither is a container type) and stay `400`. Needs to be split out of the shared parametrize (a bare `assert resp.status_code == 400` — no constant, no exception, no card — exactly the class of site the brief warned every other grep would miss) |

### Sites the greps surfaced that are NOT guardrail-inversion sites (reviewed and ruled out)

Recorded so Task 1 doesn't re-review these from scratch:

- `tests/test_tabs_form_views.py:108-116` (`test_bad_scope_raises_nesting_error`) — malformed parent/tab refs (unknown parent, tab not in parent, non-numeric ref); unrelated to depth, unaffected.
- `tests/test_tabs_form_views.py:135-142` (`test_resolve_scope_rejects_non_tabs_parent`) — a non-container (`TextElement`) parent; still rejected under the new rule ("parent is not a container"), unaffected.
- `tests/test_twocolumn_registry.py:31-37` (`test_resolve_scope_rejects_unknown_column`), `:54-59` (`test_resolve_scope_rejects_non_container_parent`) — unrelated to depth, unaffected.
- `courses/tests/test_spoiler_nesting.py:198-207` (`test_resolve_scope_rejects_wrong_slot_for_spoiler`) — wrong slot id, unrelated to depth, unaffected.
- `courses/tests/test_spoiler_nesting.py:402-413` (`test_tabs_nested_menu_still_offers_spoiler`) — still true under the new rule (spoiler is offered inside a depth-1 tabs container), not an inversion.
- `tests/test_tabs_transfer.py:131-133,145` — unknown parent / tab-not-in-parent / non-nestable child (`choice`) / parent-not-tabs cases; none touch depth, unaffected.
- `tests/test_twocolumn_transfer.py:50-68` (`test_validate_nesting_rejects_unknown_column`) — unrelated to depth.
- Unrelated `status_code == 400` sites from the same grep: `tests/test_courses_progress.py:72` (malformed JSON), `tests/test_element_try.py:98` (non-question element), `tests/test_manage_node_ops.py:427` (unknown move mode), `tests/test_ui_foundation.py:370` (invalid theme), `courses/tests/test_element_state_endpoint.py:100,120,237` (element-state POST validation) — none concern nesting depth or containers.
- `grep "nested more than one level\|may not be nested"` and the broader `"is nested\|not allowed inside a spoiler"` sweep returned **zero** matches in `tests/`/`courses/tests/` — confirms the spec's own note that the depth-cap **messages** (`payloads.py:784,797,805`) are not guarded by any existing test assertion; nothing to invert here, but Task 10/11 should know the message text is free to change without breaking a test.
- `grep "import SPOILER_CHILD_TYPES"` found exactly 3 importers: `courses/lal_loader/builders.py:95` (kept — the loader gets its own narrower constant per spec), `courses/tests/test_spoiler_nesting.py:8` (the guardrail site above, #6), `courses/transfer/payloads.py:759` (deleted with the constant, production code — Task 1's concern, not a test).
- The five direct-template-render sites needing an explicit `depth`/`max_nest_depth` seed were all found via the `_element_row\.html|_add_menu\.html` grep and match the spec's list exactly: `courses/tests/test_reveal_gate_editor_row.py:46-50`, `tests/test_tabs_editor_partial.py:70-72` and `:83-86` (also guardrail site #12 above), `tests/test_gallery_manage.py:26`, `tests/test_table_manage_plumbing.py:23`. Two additional hits from the same grep are file-reading oracles, not render call sites, per the spec: `tests/test_help.py:567` and `tests/test_tabs_editor_dnd.py:25` (both read the template source as a string; Task 8 should know about them but they take no action here).

## Caveat carried forward to Task 1 (verbatim, per the brief)

**Grepping is necessary but NOT sufficient.** Site #17 above was already known to be
findable this way (the brief's own `status_code == 400` grep was included specifically
because it surfaces exactly this class of site — a bare status-code assertion with no
named constant, exception, or card). The brief states plainly that an 18th-or-later site
of this shape could still exist and be invisible to every pattern run here. **Before
Task 1's commit, the full suite must be run against the applied diff, and every failure
treated as a candidate guardrail** — that is the only search that is actually complete.
This list is a re-derived floor, not a ceiling.

## Step 5 note

Committed as `chore(depth-3): record baseline gates and re-derived guardrail list`.
